<div class="container">
    <div class="row">
        <div class="col-md-8">
            <div id="articles">
                {{#if articles}}
                    {{#each articles}}
                        {{#if isSaved}}
                            <hr>
                            <div class="card">
                                <div class="card-body">
                                    <h5 class="card-title">{{this.title}}</h5>
                                    <p class="card-text">{{this.link}}</p>
                                    <div class="btn-group" role="group">
                                        <button class="btn btn-secondary notes" data-id="{{this._id}}">Add/View Notes</button>
                                        <button class="btn btn-danger unsave-article" data-id="{{this._id}}">Delete From Saved</button>
                                    </div>
                                </div>
                            </div>
                        {{/if}}
                    {{/each}}
                {{else}}
                    <div class="row"><h1>Looks like there are no saved articles yet...</h1></div>
                {{/if}}
            </div>
        </div>
        <div class="col-md-4">
            <div id="notes"></div>
        </div>
    </div>
</div>
